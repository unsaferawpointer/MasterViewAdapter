import XCTest
@testable import MasterViewAdapter

final class MasterViewAdapterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(MasterViewAdapter().text, "Hello, World!")
    }
}
